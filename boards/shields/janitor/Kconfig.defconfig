if SHIELD_JANITOR_LEFT

config ZMK_KEYBOARD_NAME
	default "Janitor"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

endif

if SHIELD_JANITOR_LEFT || SHIELD_JANITOR_RIGHT

config ZMK_SPLIT
	default y

config ZMK_DISPLAY
	default y

endif
